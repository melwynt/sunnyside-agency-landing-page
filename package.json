{
  "name": "sunnyside-agency-landing-page",
  "version": "1.0.1",
  "description": "Sunnyside agency landing page - Frontend Mentor Challenge",
  "scripts": {
    "start": "rm -rf public/* && parcel src/index.html --dist-dir public --port 1234 --hmr-port 4326 --host localhost --open",
    "build": "rm -rf public/* && parcel build src/index.html --dist-dir public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "prettier": {
    "singleQuote": true
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.2",
    "parcel": "latest"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  }
}
